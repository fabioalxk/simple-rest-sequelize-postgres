How to run it

Configure database
  
  Create table "items" in postgres with the following fields:
  
  id (serial), 
  
  name (character varying), 
  
  createdAt (date), 
  
  updatedAt (date)
  
> npm i

> node app.js
